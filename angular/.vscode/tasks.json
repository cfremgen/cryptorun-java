{
    "version": "2.0.0",
    "command": "gulp",
    "tasks": [
        {
            "label": "Run build",
            "type": "gulp",
            "task": "build",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}